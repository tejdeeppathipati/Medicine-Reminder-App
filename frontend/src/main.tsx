import React from 'react';
import ReactDOM from 'react-dom/client';
import MedicationSetup from './MRsetup';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <MedicationSetup />
  </React.StrictMode>
);